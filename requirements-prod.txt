# Production dependencies with exact pinned versions
# Generated: 2026-02-04
# Regenerate with: pip freeze > requirements-prod.txt

# Core dependencies (exact versions for reproducible builds)
instagrapi==2.1.3
python-dotenv==1.0.1
playwright==1.50.0
Pillow==11.0.0
Flask==3.0.3

# Dependencies of dependencies (auto-resolved)
# Run 'pip freeze' in your environment to capture exact versions
# This ensures bit-for-bit reproducible deployments

# Security note: Update these versions regularly
# Check for vulnerabilities: pip-audit or safety check
# Update workflow: test with requirements.txt first, then freeze to requirements-prod.txt
