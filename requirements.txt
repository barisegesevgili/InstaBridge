# Core dependencies with safe version ranges
# Use ~= for compatible release (patch version updates only)
# This prevents breaking changes while allowing security fixes

instagrapi~=2.1.3  # Instagram API wrapper
python-dotenv~=1.0.1  # Environment variable management
playwright~=1.50.0  # Browser automation
Pillow~=11.0.0  # Image processing
Flask~=3.0.0  # Web framework
keyring~=25.5.0  # Secure credential storage (system keychain)